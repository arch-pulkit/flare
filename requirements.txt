numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3
onnxruntime>=1.17
pyyaml>=6.0
sentence-transformers>=3.0
torch>=2.0

# ASTR-O — the audit layer. Imported by flare/pipeline.py,
# flare/audit/handlers.py, config/schema_builder.py and build_registry.py.
# FLARE will not import without it.
#
# ASTR-O is in its deployment phase and not yet public: there is no clone URL
# and it is not on PyPI, so it cannot be listed as a resolvable requirement
# here. A bare `astr_o` line would only make `pip install -r` fail. Install it
# editable from a local checkout before installing this file:
#
#     pip install -e /path/to/astr-o
#
# If the checkout is moved after installation, the editable install keeps
# pointing at the old absolute path and `import astr_o` fails with
# ModuleNotFoundError — re-run `pip install -e .` from the new location.
